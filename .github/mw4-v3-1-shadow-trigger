MW4 V3.1 canonical architecture qualification

Single executable semantic path:
evidence analysis -> direct-interaction hostile verification -> exact gap-split combat islands -> canonical story/Finishing Move planning -> integrity/quality gates -> canonical adaptive allocator -> FFV1/NUT renderer.

No historical semantic planner, detached Finishing Move body planner, fallback guard, legacy allocation contract, allocator patch layer, or allocation regression shim exists on this head.

Batch2 Finishing Move policy: protected verified hero at 1.0x, then only independently qualified verified-payoff combat islands. Intermediate body components use exact gap-split island bounds; the final island may trim only trailing context to the canonical verified-payoff tail. Explicit hard cuts omit rejected reload/search/traversal source time and never pad footage.

Every hostile event requires local direct-interaction confirmation. Contact, motion, HUD/color/minimap state and ambiguous actors remain non-hostile.

Existing editorial quality gates are unchanged. Existing source certification, exact decoded-frame hash identity, FFV1 level-3 + PCM s16le NUT transport, source-native delivery timing/metadata restoration, SSIM >= 0.99, PSNR >= 40 dB and production bitrate contracts remain mandatory.

2026-09-15 qualification trigger: canonical semantic + canonical allocation architecture, exact-island Finishing Move body composition, no alternate planner code.
