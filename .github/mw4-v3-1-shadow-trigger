MW4 V3.1 exact-island canonical semantic qualification

Single executable semantic path:
evidence analysis -> direct-interaction hostile verification -> exact gap-split combat islands -> canonical normal/Finishing Move planning -> integrity/quality gates -> canonical adaptive allocator -> FFV1/NUT renderer.

The base V3.1 module is evidence-only. The obsolete refined planner, historical semantic planner, detached Finishing Move body planner, fallback guard, legacy allocation contract, allocator patch layer, and allocation regression shim are physically absent from this head.

Batch2 Finishing Move policy: protected verified hero at 1.0x, then only independently qualified verified-payoff combat islands. Every body segment, including the terminal body island, preserves its exact canonical hard-gap-split island start and end. The semantic planner and allocation contract both prohibit payoff-centered sub-windows, terminal payoff-tail trims, minimum-duration padding, earlier-footage continuation, semantic montage, and no-payoff alternatives. Explicit bounded hard cuts omit rejected reload/search/traversal source time.

Every hostile event requires local direct-interaction confirmation. Contact, motion, HUD/color/minimap state and ambiguous actors remain non-hostile. The centered MW4 hitmarker geometry remains pinned at outer-radius fraction 0.105 and the acceptance threshold remains 0.34. Candidate-centered direct-interaction verification samples bounded windows at 60 fps so short source-native hitmarker transients are not discarded by 12 fps temporal undersampling. No hostile, editorial, duration or quality threshold is lowered.

Canonical combat-island interval checks use half-open frame-grid semantics with bounded millisecond-rounding tolerance. This prevents serialized exact island starts/ends from reabsorbing an adjacent rejected hard-gap frame while preserving true hard-gap rejection. Regression tests cover rounded-up terminal bounds, rounded-down opening bounds, and a true-gap negative.

Each analysis job prints its complete canonical semantic evidence manifest into the CI log before artifact upload, including local direct-interaction scores, exact combat-island counts and Finishing Move continuation rejection diagnostics.

Fail-closed allocation remains mandatory. If verified evidence cannot satisfy the campaign contract, allocation exits non-zero and uploads structured rejection diagnostics rather than manufacturing a clip or hiding the reason behind a traceback-only failure.

Existing source certification, exact decoded-frame hash identity, FFV1 level-3 + PCM s16le NUT staging/stitched/canonical transport, source-native delivery timing/metadata restoration, SSIM >= 0.99, PSNR >= 40 dB and production bitrate contracts remain mandatory.

2026-09-16 qualification trigger: exact half-open combat-island frame bounds, 60 fps bounded direct-interaction verification at unchanged score threshold, exact semantic evidence surfaced in logs, exact terminal bounds enforced end-to-end, hero-aware body duration, one canonical semantic route, structured fail-closed allocation evidence, no fallback implementation.

Run-64 qualification additionally exercises corrected allocation-rejection manifest extraction plus exact combat-island/orphan-hostile-event assignment diagnostics at the current canonical head.